{
  "name": "geospatialwebhook.com",
  "version": "1.0.0",
  "private": true,
  "description": "Python Geospatial Webhook & Event-Driven Architecture",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site",
    "deploy": "npm run clean && npm run build && wrangler pages deploy _site --project-name=geospatialwebhook-com --branch=main --commit-dirty=true",
    "deploy:preview": "npm run clean && npm run build && wrangler pages deploy _site --project-name=geospatialwebhook-com --branch=preview --commit-dirty=true"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "markdown-it": "^14.1.0",
    "markdown-it-anchor": "^9.2.0",
    "markdown-it-attrs": "^4.3.1",
    "markdown-it-task-lists": "^2.1.1",
    "prismjs": "^1.29.0"
  },
  "dependencies": {
    "mermaid": "^11.15.0"
  }
}
